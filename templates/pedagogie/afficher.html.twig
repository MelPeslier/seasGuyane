{% extends 'base.html.twig' %}

{% block title 'cour #' ~ cour.id %}

{% block body %}
    <h1>{{ cour.titre }}</h1>
    <p>{{ cour.descriptionBreve }}</p>
    <p>{{ cour.description }}</p>

    <img src="{{ vich_uploader_asset(cour) }}" alt="{{ cour.imageName }}">
    <p>{{ cour.imageName }}</p>

    <a href="{{ path('app_pedagogie_modifier', {id: cour.id}) }}">Modifier</a>
    <a href="#" onclick="event.preventDefault(); confirm('Voulez-vous réellement supprimer ce cour ?') && 
    document.getElementById('js-pin-delete-form').submit();">Supprimer</a>
    <a href="{{ path('app_pedagogie') }}">Retour</a>

    <form id="js-pin-delete-form" action="{{ path('app_pedagogie_supprimer', {id: cour.id}) }}" method="post" style="disply:none;">
        <input type="hidden" name="pas_un_token_csrf" value="{{ csrf_token('supprimer_cour_' ~ cour.id) }}">
        <input type="hidden" name="_method" value="DELETE">
    </form>

{% endblock %}
