<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}SEAS Guyane{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}
    </head>

    <body>

        {{ include('layouts/partials/_flash_messages.html.twig') }}

        <header>
            <nav>
                <ul>
                    <a href=" {{ path('app_intro') }} ">Intro</a>
                    <a href=" {{ path('app_accueil') }} ">Accueil</a>
                    <a href=" {{ path('app_catalogue') }} ">Catalogue</a>
                    <a href="#">Catalogue Pro</a>
                    <a href=" {{ path('app_presentation_seas') }} ">Présentation SEAS</a>
                    <a href=" {{ path('app_pedagogie') }} ">Pédagogie</a>
                    <a href=" {{ path('app_ressources') }} ">Ressources</a>
                </ul>
            </nav>
        </header>

        <div class="big_div" >
            {% block body %}{% endblock %}
        </div>
        
        <footer><p>ouiiiiiiiiiiiiiiii</p></footer>
    </body>
</html>
